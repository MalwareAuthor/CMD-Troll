
#include <iostream>
#include <cstdlib>

int main() {
    for (int i = 0; i < 1000; i++) {
        system("start cmd /C \"color 0C && echo You've been CMDRAPED && timeout /T 2 && exit\"");
    }
    return 0;
}
